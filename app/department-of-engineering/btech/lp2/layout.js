export const metadata = {
  title: "Admissions 2026 | Tulas University, Dehradun",
  robots: {
    index: false,
    follow: true,
  },
};

export default function Layout({ children }) {
  return children;
}
