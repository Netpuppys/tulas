export const metadata = {
  title: "Thank You | Tulas University, Dehradun",
  robots: {
    index: false,
    follow: true,
  },
};

export default function Layout({ children }) {
  return children;
}
